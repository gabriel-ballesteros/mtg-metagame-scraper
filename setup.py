from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A data scraping project to collect mtg metagame data',
    author='Gabriel',
)
